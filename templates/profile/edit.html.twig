{% extends 'base.html.twig' %}

{% block title %} Modification du profil {% endblock %}

{% block body %}
    {% include '_parts/_forumHeader.html.twig' %}

    <main class="container h-100">
        <h2 class="mt-2 mb-4">Modifier mon profil</h2>
        <section class="m-2">
            <div class="card ms-3">
                <div class="card-body">
                    <p class="card-title h5">Ce que je peux modifier</p>
                    <p class="card-text text-center">
                        <a href="{{ path('profile_edit_password')}}" class="btn btn-primary m-5">Modifier mon mot de passe</a>
                        <a href="{{ path('profile_edit_email')}}" class="btn btn-info m-5">Modifier mon adresse email</a>
                    </p>
                    <p class="card-text">
                        
                    </p>
                </div>
            </div>
        </section>    
    </main>

    {% include "_parts/_footer.html.twig" %}

{% endblock %}