<form method="post" action="{{ path('user_app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sur de vouloir supprimer votre compte ?);">
    <input type="hidden" name="_token" value="{{ csrf_token('delete') ~ user.id }}">
    <button class="btn btn-danger">Supprimer le compte {{ user.nickname }}</button>
</form>