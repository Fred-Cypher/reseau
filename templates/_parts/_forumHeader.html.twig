<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid column">
            <div>
                <div>
                    <h1>Réseau social</h1>
                    <a href="{{ path('main') }}"><p>Logo Réseau / Accueil</p></a>
                    <p>Bienvenue sur la partie privée notre réseau social</p>
                </div>
            
                <div class="navbar-nav">
                    <ul class="navbar-nav">
                        {% if app.user %}
                        <li class="nav-item">
                                <a class="nav-link" href="{{ path('main') }}">Retour à l'accueil</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('profile_index') }}">Mon compte</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>    
            <div>
                <div class="navbar-nav">
                    <ul class="navbar-nav">
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('forum_index') }}">Retour à l'accueil du forum</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('images_images') }}">Partage d'images</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('articles_articles') }}">Partage d'articles</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </div>
        <div>
            Compte : {{ app.user.nickname }}
        </div>
    </nav>
    
</header>