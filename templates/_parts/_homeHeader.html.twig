<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <div>
                <h1>Réseau social</h1>
                <a href="{{ path('main') }}"><p>Logo Réseau / Accueil</p></a>
                <p>Bienvenue sur notre réseau social</p>
            </div>
            
            <div class="navbar-nav">
                <ul class="navbar-nav">
                    {% if app.user %}
                    <li class="nav-item">
                            <a class="nav-link" href="{{ path('forum_index') }}">Accès au forum</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('profile_index') }}">Mon compte</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Me connecter</a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>

        {% if app.user %}
            <div>
                Compte : {{ app.user.nickname }}
            </div>
        {% else %}
            <div>
                Compte : aucun utilisateur connecté
            </div>
        {% endif %}
        
    </nav>

</header>