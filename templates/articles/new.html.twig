{% extends 'base.html.twig' %}

{% block title %}Nouvel article{% endblock %}

{% block body %}
    <section class="container">
        <h2>Enregistrer un nouvel article</h2>
        <article class="mt-3">
            {{ form_start(articlesForm) }}
                {{ form_row(articlesForm.title) }}
                {{ form_row(articlesForm.summary) }}
                {{ form_row(articlesForm.content) }}
                <button type="submit" class="btn btn-primary mt-3 btn-lg">Enregistrer l'article</button>
            {{ form_end(articlesForm) }}
        </article>
    </section>

    <a href="{{ path('app_articles') }}">Retour à la liste d'articles</a>
{% endblock %}
